# TikTok B2B Bot — 开发者 WIKI 索引

> 版本: 0.6.5-hermes-live-h4a | 最后更新: 2026-08-09

## 变更摘要（v0.6.5-hermes-live-h4a）

- 新增按 Job 隔离的持久化 Pipeline 事件和决策关卡底座：全局不复用 sequence、
  `afterSequence` 增量读取、数据库级单 Job 单 pending、resolve/cancel CAS 和旧库兼容均有测试
- 事件与关卡使用严格字段类型、长度、结构和敏感键校验；错误终态只能保存注册错误码对应的
  公开文案，不保存异常正文、凭据、Profile 或模型 Prompt/Response
- 高频 `scroll/wait` 采用 append-only 有界抑制，关键事件不丢；独立 50ms SQLite 遥测连接
  在业务事务持锁时快速 fail-open，不改变调用方事务或业务结果；专项与相邻回归 **142 passed**
- 本阶段尚未接 Runner、10 秒默认状态机、实时 API 或前端作战窗口，不提前宣称交互功能可用

## 变更摘要（v0.6.4-hermes-search-bootstrap）

- 修复真实抖音获客阶段 01 从 `about:blank` 开始、完全依赖模型猜测搜索入口的问题：
  `Platform` 新增关键词视频搜索 URL，Hermes 在第一次 LLM 决策前确定性进入同平台、已编码的
  视频搜索页，并把该导航计入页面和时间预算
- 视频浏览 Agent 达到 `max_steps` 且未产出证据时，仍执行已确认的直接用户辅助搜索；只有
  所有计划关键词都耗尽步数且视频、用户证据均为零时，阶段才以
  `search_exhausted_without_evidence` 失败关闭，不再把技术超时显示成获客成功
- 导航前、浏览器初始化后及导航后继续执行平台域名校验；如果浏览器已落到外域，不允许
  通过新的启动 URL“恢复”后继续运行。关键词按完整 URL 组件编码，含 `/ ? #` 也不会改变
  搜索路径结构。DeepSeek 单次非法 JSON 作为受 step/LLM 双预算约束的无效步骤继续决策，
  不再让整词搜索立即失败。页面预算以实际访问 metrics 为准，不再把同页提取的证据链接
  误算成额外页面。单次网络、超时或上游服务故障也在原有预算内继续下一步。上述根因与
  预算耗尽场景共新增 11 个测试项，后端全量 **751 passed**

## 变更摘要（v0.6.3-hermes-h2）

- `/pipeline` 的旧普通 Job 表单已替换为四步 AI 获客创建器：执行环境、目标画像、探索策略、
  确认创建；历史、详情、取消、重试和轮询仍由唯一 Pipeline 页面管理
- TikTok 必须配置目标国家，抖音固定中国；`collect` 不可取消，平台、账号和 Browser
  Provider 预检通过后才能继续
- 硬性条件与阶段 02 核验偏好分区展示；员工数、注册资本和上市状态明确不作为阶段 01
  强制淘汰条件；关键词、70/30 组合和七项探索预算均使用与后端一致的边界校验
- 前端只调用原子 `POST /api/acquisition/jobs`，并发点击只发送一次；成功摘要深拷贝服务端
  `job/campaign/keywords` 响应，后续编辑不会污染旧任务快照
- 桌面与 390px 真实后端页面验收通过；移动端应用壳改为可横向浏览的底部导航，页面无横向
  溢出，最小操作高度 44px。后续回归进一步把导航滚动区与退出区彻底分离，移除模型配置页
  对全局侧栏的样式覆盖，并默认关闭会遮挡窄屏入口的 Vue DevTools 浮钮。前端 Vitest
  `74 passed`、Smoke `134 passed`、后端全量
  `733 passed`；H3 阶段结果/人工复核工作台和租户隔离仍未交付
- 抖音平台头像不再长期依赖会过期的 CDN 签名 URL：登录核验/账号检测即时缓存受限图片字节，
  账号 API 派生 `avatar_data_url`，前端优先显示本地缓存；现有账号已重新检测并恢复真实头像。

## 变更摘要（v0.6.2-hermes-h1）

- 新增受认证的 `POST /api/acquisition/jobs`：Pipeline Job、Campaign 和 1—100 个初始关键词
  在同一事务提交，避免 Dispatcher 先认领到缺少获客定义的 queued Job
- AI 获客阶段必须是六阶段的严格有序子序列且包含 `collect`；`configSnapshot` 在 API 与
  Service 两层递归拒绝凭据类键，错误不回显敏感键值
- AI 获客重试统一从 `collect` 开始，并在同一事务克隆 Campaign 与关键词定义；新执行的
  使用次数、视频数、候选数等计数从 0 开始，legacy 重试语义保持不变
- `BusinessReadModel` 将任务级资格、分类、评分和证据投影到 Users、Lead、Dashboard、
  Reports Overview 与关键词图表；AI 结论不反写全局 `User.status/category`
- 关键词效果按 AI Evidence 与 legacy 来源词的不同用户实时聚合，不信任可能过期的计数器；
  Lead 直接使用 0..100 AI 匹配分，报告和词云返回真实数据契约
- H1 后端全量回归 `733 passed`；租户隔离仍是 P0 上线阻断，H2 创建表单和 H3 复核工作台
  不在本阶段范围内

## 变更摘要（v0.6.1-stage02）

- 阶段 02 使用公开字段白名单、严格资格契约、双评分、多标签和四状态人工复核；AI 的通过
  与淘汰都只作为建议，只有人工操作能写 `qualified/rejected`
- 人工版本 CAS 防止耗时 LLM 结果覆盖人工通过/淘汰；候选与策略使用 keyset 分页，移除
  固定前 200/100 条造成的饥饿
- Campaign 策略采用严格 `StrategyResult`；模型自由话术由确定性中性模板替换，危险或无效
  输出不落库、不触达；阶段 03/04 均再次检查当前 Job、平台和人工合格状态
- 多账号数据隔离经只读审计升级为 P0 上线阻断，明确 T1—T5 实施顺序；本版本尚未宣称
  多租户安全

## 变更摘要（v0.5.9）

- 🧭 **失效登录态闭环**：前端收到受保护 API 的 401 后会清除浏览器中的旧 token，
  自动跳转登录页并保存安全的站内返回地址；重新登录后回到原配置页面
- 🛡️ **避免登录循环与开放重定向**：用户名密码错误仍在登录页原位提示；返回地址只接受
  单斜杠开头的站内路径，拒绝外部 URL、双斜杠与 `/login`
- 🔄 **并发 401 安全**：只允许失败请求携带的 token 清理同一枚当前 token；旧请求迟到
  时不会删除用户刚登录取得的新 token
- 🧪 **前端专项验收**：失效会话和模型配置组件 14 passed

## 变更摘要（v0.5.8）

- 🔐 **修复重启后模型保存提示登录**：认证模块不再完全依赖 Uvicorn 的
  `--env-file` 参数；进程未提供 `JWT_SECRET` 时会从项目根目录、Git 忽略的 `.env`
  读取稳定密钥，避免 API 重启后浏览器旧 token 因随机签名密钥而失效
- ✅ **现场恢复**：API 已按 `.env` 重启，带签名令牌访问 LLM Provider 列表返回 200；
  认证与 LLM 管理相关回归 14 passed

## 变更摘要（v0.5.7）

- 🛠️ **模型编辑器可见性修复**：`/config-llm` 的新增和编辑 Provider 改为视口内居中
  对话框，支持背景关闭、Escape 和移动端底部布局，不再把表单静默展开到页面下方
- 👤 **账号展示名与真实头像**：账号新增独立 `display_name` 本地备注，浏览器隔离
  `username` 保持不可变；抖音登录和账号检测会从已验证的官方同源资料探针同步平台昵称、
  HTTPS 头像与粉丝数，账号卡片和活动列表优先展示真实头像
- 🛡️ **检测边界收口**：TikTok 尚无可靠身份探针时返回 `session_check_unsupported` 并
  保持原账号状态；抖音探针未返回某项资料时保留数据库旧值，不再把未知粉丝数覆盖为 0
- 🧪 **阶段验收**：后端新增/迁移/平台边界专项 7 passed，前端视图组件 12 passed，相关
  后端回归 281 passed，Smoke 129 passed；类型检查、生产构建与本地真实 API/浏览器页面检查通过

## 变更摘要（v0.5.6）

- 🔐 **LLM 管理鉴权收口**：9 个 Provider/Route/Usage 端点全部要求 JWT 或 API Key；
  旧 `/api/config/apikey` 兼容入口同样强制认证并复用安全 Secret 写入；配置响应不再暴露
  密钥末四位；CORS 只接受显式 http(s) 来源，默认限本机 UI，拒绝通配符
- 🧱 **单 worker 运行边界**：API 生命周期通过 `data/api-worker.lock` 阻止第二个 worker，
  Docker 固定 `--workers 1`，避免进程内 LLM Router、Pipeline 和浏览器状态互相漂移
- 🛠️ **保存与登录可靠性**：前端每次请求读取最新 token；Provider 元数据已创建但密钥
  写入失败时保留其 id，重试走更新而非重复创建；开发 Auto 模式默认连接真实后端
- 🧪 **阶段验收**：LLM 后端专项 103 passed、扩展相关回归 270 passed、前端组件 8 passed、
  Smoke 128 passed；旧密钥入口测试使用临时数据库与临时 `.env`，不会改写用户配置；
  类型检查、生产构建及桌面/390px 真实后端浏览器检查通过

## 变更摘要（v0.5.5）

- ✨ **LLM 管理 API 完成**：Provider CRUD、服务端连接测试、独立密钥更新、五类 Route
  原子替换与真实用量聚合均已接入 SQLite 权威数据源
- 🔐 **密钥边界收口**：API 响应从不返回密钥；前端编辑时不回填旧值；新值只写入
  Provider 指定的环境变量和项目根目录下被 Git 忽略的 `.env`，并拒绝换行注入
- ✨ **真实模型配置 UI 完成**：`/config-llm` 支持 DeepSeek、OpenAI、通义千问和自定义
  Provider，服务端测试连接、五条有序业务路由、真实用量、完整错误/空/加载状态及中英文
- 🧪 **阶段验收**：后端相关回归 100 passed，前端组件 7 passed，Smoke 126 passed；
  TypeScript、生产构建、桌面与 390px 响应式浏览器检查通过

## 变更摘要（v0.5.4）

- ⚠️ **修复抖音用户入库**：搜索卡片 href 是 `/user/<sec_uid>` 不是 @handle，
  旧代码直接把 sec_uid 当 username 入库，nickname/follower/bio 全空
- ✨ **`_extract_user_meta`**：从同一卡片 DOM 抽取昵称、粉丝数、签名；username
  优先用 `@unique_id`（无则回退 sec_uid，保持 stable key）
- 🧪 **5 个新单测**：抖音/TikTok 卡片抽取、handle 缺失回退、空 DOM 兜底

## 变更摘要（v0.5.3）

- ⚠️ **修复 LLM Provider 保存**：原 `POST /api/config/apikey` 硬编码写 `DEEPSEEK_API_KEY`，
  与 Settings 实际读取的 `LLM_API_KEY` 不一致，UI 保存后 Router 仍拿到占位 key
- ⚠️ **修复 .env 写入路径**：`__file__.parents[2]` 指向项目父目录而非项目根，
  导致 `.env` 写入失败；改为 `parents[1]`
- ⚠️ **修复 GET /api/config 缓存**：模块级 `settings` 变量在 reload_settings 后仍指向
  旧对象，改为每次 GET 实时调 `get_settings()`
- ✨ **响应携带 `envVar`**：保存成功后告诉调用方 key 实际写到哪个环境变量名，
  便于排查多 Provider 场景

## 变更摘要（v0.5.2）

- ✨ **Hermes 浏览器 Agent**：`BrowseAgent` 实现「截图→LLM→动作」最小闭环，
  走 `iteration` LLM 路由；动作集合固定为 6 个（navigate/click/scroll/wait/extract/done）
- ✨ **`tiktok-bot browse run` CLI**：可指定平台、账号 ID、目标与最大步数；
  输出 JSON 含每步 action、rationale、screenshot hash
- ✨ **`skills/tiktok-browse/` Skill**：让 Hermes 能直接调用驱动浏览器做获客探索
- ⚠️ **安全边界**：navigate URL 必须 http(s) 且 host 落在抖音/tiktok 受信任域；
  click 必须给非空 selector；不安全决策记录为 invalid step 继续循环而不是中断
- ✨ **事件总线扩展**：`BROWSE_STEP`（高频 fire-and-forget）和 `BROWSE_DONE`
  （同步 publish）事件类型供上层订阅

## 变更摘要（v0.5.1）

- ⚠️ **修复抖音交互登录验证**：探针 URL 补齐 `device_platform=webapp&aid=6383`
  等 webapp 客户端参数；裸 URL 对已登录会话也会返回 `status_code=8`
- ⚠️ **放宽抖音 Cookie 门槛**：抖音 PC web 已登录时并不下发 `LOGIN_STATUS`，
  缺失不能判为未登录；只有平台明确下发 `LOGIN_STATUS=0` 才算离线
- ⚠️ **修复账号检测**：先导航到 `https://www.douyin.com/` 再注入 Cookie
  （about:blank 下 secure cookie 被拒）；判据改为服务端探针而非主页 URL 含
  "login"；DetachedInstanceError 不再被静默吞掉
- ✨ **诊断码透传**：Provider 产出的 `profile_*`、`cookie_consistency_failed`、
  `homepage_*` 诊断码加入 API 白名单，前端可显示具体失败原因；上一次失败
  的诊断码不会残留到成功会话

## 设计文档

- [Hermes 实时作战窗口与交互关卡设计](../plans/2026-08-09-hermes-live-command-center-design.md)
  — 将 Pipeline 的阶段 JSON 详情升级为按 Job 隔离的实时作战窗口；使用持久化事件、
  10 秒服务端默认决策、阶段 01—02 业务看板和候选人工复核工作台。
- [Hermes 实时作战窗口与交互关卡实施计划](../plans/2026-08-09-hermes-live-command-center.md)
  — 按持久化事件、决策状态机、实时 API、作战窗口、复核工作台和真实浏览器验收拆分的
  TDD 交付任务与备份节点。
- [Hermes H2 获客任务创建器设计](../plans/2026-08-08-hermes-h2-acquisition-creator-design.md)
  — 将目标画像、条件分层、初始关键词和搜索预算嵌入现有 Pipeline 创建区，
  并接入 H1 原子获客建单端点；H2 不包含 H3 结果看板和人工复核工作台。
- [Hermes H2 获客任务创建器实施计划](../plans/2026-08-08-hermes-h2-acquisition-creator.md)
  — 前端原子契约、画像校验、四步创建器、Pipeline 嵌入、页面验收与 H2 备份的 TDD 拆分。
- [Hermes H1 数据接线设计](../plans/2026-08-04-hermes-h1-data-integration-design.md)
  — 原子 AI 获客建单与重试、统一业务读模型，以及现有 Pipeline/Users/Lead/Dashboard/Reports
  的同源数据口径；H1 不包含 H2/H3 前端工作台。
- [Hermes H1 数据接线实施计划](../plans/2026-08-04-hermes-h1-data-integration.md)
  — 原子建单、AI 重试连续性、业务用户投影、关键词/Lead/报告接线和回归验证的 TDD 拆分。
- [阶段 01—02 Agent 编排设计](../plans/2026-08-02-stage01-02-agent-orchestration-design.md)
  — 视频/评论优先发现、目标画像、关键词闭环、来源证据、双评分、四状态、人工复核闸门，
  以及 Hermes 受限浏览 Agent 与现有 Pipeline 的职责边界。
- [阶段 01—02 Agent 编排实施计划](../plans/2026-08-02-stage01-02-agent-orchestration.md)
  — 数据层、API、Agent、资格判断、业务化结果页和阶段验收的 TDD 任务拆分。
- [多账号租户数据隔离安全债务](../plans/2026-08-01-tenant-data-isolation-backlog.md)
  — 已确认登录账号目前只是鉴权身份，社媒账号、客户、任务和 LLM 配置尚未形成租户边界；
  作为高优先级安全待办，在大模型认证阶段验收后单独实施。
- [交互式登录与统一 LLM Router 设计](../plans/2026-07-28-interactive-login-and-llm-router-design.md)
  — 账号级持久化浏览器、人工风控验证、登录态复用，以及项目内部唯一的 LLM 路由。
- [交互式登录与统一 LLM Router 实施计划](../plans/2026-07-28-interactive-login-and-llm-router.md)
  — 登录与大模型两条主线的 TDD 任务、精确文件范围、阶段验收与回归命令。
- [全局统一管线任务系统设计](../plans/2026-07-26-unified-pipeline-job-system-design.md)
  — 单一任务模型、双平台 Provider、持久化调度、并发控制与统一 UI。
- [全局统一管线任务系统实施计划](../plans/2026-07-26-unified-pipeline-job-system.md)
  — TDD 任务拆分、精确文件范围、验证命令与交付门槛。

## 分阶段实施状态（v0.5.0）

- ✅ **抖音交互登录核心 Provider 已完成**：账号级隔离的有头持久化浏览器只打开抖音
  首页，由用户在真实浏览器中完成扫码、短信或滑块；核心层不自动点击、不提取二维码，
  也不再使用整页截图充当二维码
- ✅ **登录态核心持久化已完成**：抖音保存独立 Playwright Profile 与含 IndexedDB 的
  storage state；路径防碰撞、私密目录权限、账号租约与 `LoginSession` 状态机已落地
- ✅ **统一交互登录核心 Service 已完成**：`start/status/verify/cancel/aclose` 统一管理
  start、共享 verify operation、超时、浏览器资源和账号租约；挂起操作可取消，关闭失败
  保留租约并支持重试
- ✅ **提交边界与关闭语义明确**：账号更新事务启动后由事务结果决定 confirmed/failed，
  timeout/cancel 不伪造回滚；pending commit/open reaper 或清理失败时 shutdown 返回
  `login_cleanup_incomplete` 并保持可重试 closing 状态
- ✅ **TikTok 指纹边界已完成**：仅 nominal 指纹适配器可通过专用 Registry 入口注册，
  且必须与账号 Provider/Profile 绑定；普通 Playwright 和字符串伪装均 fail-closed
- ✅ **交互登录 API 核心已完成**：四个会话端点统一调用 `InteractiveLoginService`，
  生产接线复用浏览器 Registry，登录会话内部共用 `AccountLeaseManager`；响应采用
  凭据白名单，账号更新在单个事务中提交，服务关闭会执行 `aclose()`
- ✅ **账号 alias 规范与冲突边界已统一**：添加账号、登录请求、resolver、浏览器隔离键
  和 updater 共用 NFKC + 首尾空白清理规则；SQLite 写入以 `BEGIN IMMEDIATE` 串行化
  扫描与提交，历史等价 alias 多行时稳定返回 `account_alias_conflict`，不任取一行
- ✅ **首次登录遵守全局账号上限**：新 alias 在打开浏览器前执行只读容量预检，并在
  updater 的 `BEGIN IMMEDIATE` 事务中再次检查，防止并发或 TOCTOU 绕过 `MAX_ACCOUNTS`；
  已有账号在满额时仍可更新，满额新账号稳定返回 `account_limit_reached`
- 🚧 **Pipeline 账号租约接线仍待后续 Task**：当前登录会话已具备稳定的账号互斥，
  但 Pipeline/登录共用同一个 `AccountLeaseManager` 尚未完成，不宣称已跨系统互斥
- ✅ **旧 QR API 已安全降级**：旧创建/状态端点转发同一 Service 并标记 deprecated，
  不再返回二维码或路径字段；图片端点固定返回 410 且不读取文件
- ✅ **登录 UI 已迁移为人工浏览器流程**：`InteractiveLoginModal.vue` 只展示平台、
  alias、人工验证步骤和安全状态；用户明确点击验证后才保存，非 confirmed 会话在关闭、
  切平台或卸载时幂等取消，页面不再渲染二维码、截图、token 或凭据
- ⏳ **具体 TikTok 厂商适配待后续阶段**：当前只有严格接口与不可用占位，仍需接入
  真实指纹浏览器 Profile；绝不回退普通 Playwright
- ✅ **LLM Router 数据层已完成**：`llm_providers / llm_routes /
  llm_request_logs` 成为 Provider、五类业务 Route 与用量日志的唯一权威源；首次启动
  从 legacy Settings 幂等创建 `legacy-default`，只保存 `LLM_API_KEY` 环境变量名
- ✅ **LLM Router 执行核心已完成**：按数据库优先级加载 Provider 链，单次最多尝试
  三家；只对可重试故障执行 fallback，连续三次失败熔断并在 60 秒后单探针半开；
  SDK 内部重试已关闭，Client 随配置或环境变量密钥变化安全刷新并确定性关闭；
  SQLite 访问不阻塞事件循环，请求日志失败不改变业务结果且不保存内容或密钥
- ✅ **LLM 管理 API 与真实 UI 已完成**：Provider/密钥/Route/用量均走真实后端；连接测试
  由服务端执行，不依赖 cc-switch 常驻，也不存在前端“主供应商”第二状态
- 🚧 **LLM 业务调用点显式 Route 映射仍待 Task 9**：现有 Collector、Filter、策略和迭代
  调用尚需分别固定映射到 `collection/qualification/strategy/iteration`；未完成前不宣称
  五条业务路由已全面接管 Pipeline 调用

## 变更摘要（v0.4.0）

- ✨ **统一 Pipeline 任务系统**：TikTok 与抖音共用
  `pipeline_jobs / pipeline_job_stages / pipeline_schedules`、同一组 REST API、
  同一个 `/pipeline` 页面和同一份任务历史；手动与定时执行都进入 SQLite 队列
- ✨ **平台与账号选择统一**：任务和计划都显式保存 `platform`、
  `accountMode(auto/specified)`、`accountId` 与阶段列表；CLI 也只创建同一种持久化任务
- ✨ **运行边界明确**：抖音每个任务使用独立 Playwright Context，并按
  `douyin_max_concurrency`（1..20）限制并发；TikTok 必须配置指纹浏览器 Provider，
  默认返回 `fingerprint_provider_unavailable`，不会回退到普通 Playwright
- ✨ **可靠执行**：支持阶段持久化、取消、从失败阶段重试、启动恢复和任务内用户/
  策略/消息隔离；触达预留保证同一任务不会自动重复发送不确定消息
- ✨ **统一计划管理**：五段 cron + 时区 + 启停的计划 CRUD；调度器只补建最近一次
  错过的运行，创建出的仍是普通 Pipeline Job
- ✨ **主页链接持久化**:`users.profile_url` (VARCHAR 500) + 幂等迁移;
  后端按 platform + username 兜底拼接,前端 Users / UserDetail 表格直接渲染可点外链
- ✨ **聚合统计同源**:`/api/users/stats` 增加 `rejected` / `new_today` / `by_persona`,
  前端 subtitle / KPI / 状态 chip / 画像 chip 全部走单一权威源,不再"碰巧对得上"
- ✨ **真实分页**:`/api/users?limit=&offset=` 的 `total` 用 SQL count(忽略 limit/offset),
  与 stats.total 同源,翻页严格一致
- ✨ **手动添加对话框**:从页面底部改回居中弹窗(复用 design-system 已有 `.qr-overlay`)
- ✨ **国家映射补全**:Countries 表从 10 个扩展到 20 个,覆盖 mock 生成器全部国家
- ⚠️ **修复**:`get_user_detail` 在 session 外访问 ORM 字段触发 `DetachedInstanceError` —
  把响应构造搬进 `with db.session()` 块,所有 `user.* / messages.*` 字段一次性读取

## 变更摘要（v0.3.0）

- ✨ **新增 UI 页面**：Leads（Lead 发现）、UserDetail（用户详情）、Pipeline、ConfigAccounts、ConfigLlm、ConfigPipeline、Login、NotFound
- ✨ **API 扩展**：现有 FastAPI 应用共 43 个路由装饰器（含统一 Pipeline Job/Schedule 路由）
- ✨ **手动添加用户**：`POST /api/users` 端点 + 前端表单
- ✨ **数据库补全**：`accounts` 表（系统登录）+ `tiktok_accounts` 表完整文档化
- 🗃️ **历史抖音登录实现**：曾使用弹窗选择器与 localStorage 检测；v0.5.0
  交互登录 Provider、统一会话 API 与人工浏览器 UI 已完成替换。后续仅保留具体
  TikTok 指纹厂商适配、Pipeline 共用账号租约等明确边界
- ✨ **前端交互闭环**：所有按钮绑定真实 API（删除/检测/退出/批量检测/导入 Cookie/导出日报/测试连接/切换 Provider）

## 变更摘要（v0.2.0）

- ✨ **新增双平台支持**（TikTok + 抖音）— 通过 `Platform` 抽象自动切换 URL/选择器
- 🗃️ **历史 QR 登录实现**（Playwright QR Code 流程 + Cookie 持久化）— 已由
  v0.5.0 的人工交互式持久化浏览器方案逐步替换
- ✨ **数据库升级**：`users` 表加 `platform` 列；`tiktok_accounts` 表加 `platform` 列和 `status` 字段
- ⚠️ **API 变更**：原 `/api/tiktok/accounts` 重构为 `/api/accounts`，新增 login-qrcode 等端点

## 导航

| 编号 | 文档 | 内容 |
| --- | --- | --- |
| 01 | [项目概述](01-项目概述.md) | 一句话描述、核心目标、技术栈 |
| 02 | [架构设计](02-架构设计.md) | 三层模型、依赖方向、借鉴 ChopperBot |
| 03 | [Core 层详解](03-Core层.md) | 数据模型、存储层、事件总线、扩展注册器、配置、LLM |
| 04 | [Plugin 层详解](04-Plugin层.md) | Collector/Channel/Filter 三类插件实现 |
| 05 | [Pipeline 编排](05-Pipeline.md) | 统一 Job/Stage/Runtime、执行隔离与可靠性 |
| 06 | [CLI-API-UI 接口](06-CLI-API-UI.md) | 统一任务 API、CLI、计划与页面 |
| 07 | [数据库设计](07-数据库.md) | SQLite 任务表、业务表与 ChromaDB 集合 |
| 08 | [Hermes Skills](08-Skills.md) | Skills 薄包装设计、文件结构 |
| 09 | [开发指南](09-开发指南.md) | 环境搭建、添加插件、测试策略 |
| 10 | [账号管理](10-账号管理.md) | 交互式登录、账号隔离、登录态持久化与阶段状态 |
| 11 | [双平台支持](11-双平台支持.md) | Platform 抽象、TikTok 指纹边界与抖音交互 Provider |
| 12 | [测试报告](12-测试报告.md) | 历史基线与 Task 3–11 分阶段增量验收结果 |
| 13 | [外部资料](13-外部资料与策略参考.md) | **新** TikTok/抖音 反爬策略 + MediaCrawler 架构研究 |

## 阅读建议

- **快速了解：** 01 + 02
- **写代码：** 03 + 04 + 05
- **加功能：** 04（插件）+ 09（指南）
- **部署：** 06（CLI/API）+ 08（Skills）
- **账号与平台：** 10（登录）+ 11（双平台扩展）
