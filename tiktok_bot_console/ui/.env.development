# Auto 默认连接真实后端；仅在后端不可达时回退到本地 Mock 并明确提示。
# 需要固定演示数据时，可在顶部数据源选择器中手动切换到 Mock。
VITE_USE_MOCK=false
VITE_API_BASE=http://localhost:8000
# 默认关闭浮动 DevTools，避免遮挡窄屏底部导航；需要调试时显式改为 true。
VITE_ENABLE_DEVTOOLS=false
